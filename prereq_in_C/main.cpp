#include <stdio.h>
#include <string.h>
#include "my.h"
#include "node.h"

int main()
{
	/*char * str = "and";
	t_node* node = new_node(str,NULL,NULL,NULL);
	printf("%s", node->type);
	my_str("\n");*/
	/*char * course = "mike";
	char** list[10][10];
	(*list) = "ha";

	for(int i =0; list[i]!= NULL; i++)
	{
		if (my_strcmp(course,list[i]) == 0)
			return true;
	}
	return false;
	return 1;*/
	return 1;
}